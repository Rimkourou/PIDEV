{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
<div class="container"> <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ produit.Qte }}</td>
            </tr>
            <tr>
                <th>IdCategorie</th>
                <td>{{ produit.Categorie }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img id="img"  class="card-img-top img-fluid" style="height: 50px; width: 50px;" src="/img/{{produit.image}}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">back to list</a>

    <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit/_delete_form.html.twig') }}</div>
{% endblock %}
