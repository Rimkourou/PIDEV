<form method="post" action="{{ path('panier_produit_delete', {'panierId': panier_produit.panierId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier_produit.panierId) }}">
    <button class="btn">Delete</button>
</form>
