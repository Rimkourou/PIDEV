<form style="display: inline" method="post" action="{{ path('salle_de_cinema_delete', {'id': salle_de_cinema.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_de_cinema.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
