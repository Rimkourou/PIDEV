{{ form_start(form) }}
  {{ form_widget(form.nom,{ 'label':'','attr':{'class':'form-control','placeholder':' nom *'}}) }}
    {{ form_errors(form.nom) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
